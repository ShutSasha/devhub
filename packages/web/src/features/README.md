# In general just for slices

```
├── features/                # Redux features (slices)
│   ├── user/
│   │   ├── userSlice.ts     # Example Redux slice
│   │   └── userTypes.ts     # Types for the slice
│   └── auth/
│       ├── authSlice.ts     # Example Redux slice for authentication
│       └── authTypes.ts     # Types for auth slice
│
```
